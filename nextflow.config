manifest {
    name = 'export_large_annotation_regions'
    description = 'Export large annotation regions from QuPath projects in headless mode'
    author = 'WEHI SODA Hub'
    version = '0.1.0'
    mainScript = 'main.nf'
    nextflowVersion = '!>=24.04.0'
}

process {
    executor = 'local'
    cpus = 4
    memory = '32 GB'
    time = '12h'
}

docker.enabled = false
singularity.enabled = false
conda.enabled = false

profiles {
    standard {
        process {
            executor = 'slurm'
            queue = 'regular'
            withLabel: 'process_light' {
                cpus   = { 2 }
                memory = { 8.GB }
                time   = { 1.h }
            }
            withLabel: 'process_heavy' {
                cpus   = { 48 }
                memory = { 450.GB * task.attempt }
                time   = { 48.h }
            }
        }
        executor {
            queueSize = 10
            submitRateLimit = '10 sec'
        }
    }

    conda {
        conda.enabled = true
        docker.enabled = false
        singularity.enabled = false
        process.conda = 'conda-forge::openjdk=17'
    }

    docker {
        docker.enabled = true
        conda.enabled = false
    }

    singularity {
        singularity.enabled = true
        singularity.autoMounts = true
        conda.enabled = false
        docker.enabled = false
        apptainer.enabled = false
    }

    apptainer {
        apptainer.enabled = true
        apptainer.autoMounts = true
        conda.enabled = false
        docker.enabled = false
        singularity.enabled = false
    }

    small {
        process {
            executor = 'slurm'
            queue = 'regular'
            withLabel: 'process_light' {
                cpus   = { 2 }
                memory = { 16.GB }
                time   = { 1.h }
            }
            withLabel: 'process_heavy' {
                cpus   = { 16 }
                memory = { 128.GB * task.attempt }
                time   = { 48.h }
            }
        }
        executor {
            queueSize = 10
            submitRateLimit = '10 sec'
        }
    }

    medium {
        process {
            executor = 'slurm'
            queue = 'regular'
            withLabel: 'process_light' {
                cpus   = { 2 }
                memory = { 16.GB }
                time   = { 1.h }
            }
            withLabel: 'process_heavy' {
                cpus   = { 48 }
                memory = { 480.GB * task.attempt }
                time   = { 48.h }
            }
        }
        executor {
            queueSize = 10
            submitRateLimit = '10 sec'
        }
    }

    large {
        process {
            executor = 'slurm'
            queue = 'regular'
            withLabel: 'process_light' {
                cpus   = { 2 }
                memory = { 16.GB }
                time   = { 1.h }
            }
            withLabel: 'process_heavy' {
                cpus   = { 64 }
                memory = { 1200.GB * task.attempt }
                time   = { 48.h }
            }
        }
        executor {
            queueSize = 10
            submitRateLimit = '10 sec'
        }
    }
}
